# Frontend environment template
# Copy to `.env` and adjust before running `npm run dev`.

# Backend FastAPI base URL (no trailing slash). The frontend's apiClient
# defaults to http://127.0.0.1:8000/api when this is not set.
VITE_API_BASE_URL=http://127.0.0.1:8000/api